export * from './eventSourceWrapper';
export interface API {
    // createConversation(title?: string): Promise<Conversation>
    // updateConversation(id, data): Promise<Conversation>
    // deleteConversation(id): Promise<void>
    // getConversation(id): Promise<Conversation>
    // getConversations(): Promise<Conversation[]>
    // addMessage(conversationId, message): Promise<Message>
    // updateMessage(messageId, data): Promise<Message>
    // deleteMessage(messageId): Promise<void>
    // getMessages(conversationId, pagination): Promise<Message[]>
    // uploadFile(file: File): Promise<FileUploadResponse>
    // downloadFile(url: string): Promise<Blob>
    // abortRequest(): void
}